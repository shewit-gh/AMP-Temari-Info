import 'package:flutter/material.dart';

class UnivAdmin extends StatelessWidget {
  static const String routeName = "/univAdmin";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('UnivAdmin page'),
      ),
    );
  }
}
